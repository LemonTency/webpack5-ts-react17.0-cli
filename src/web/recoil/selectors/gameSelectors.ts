import { selector } from 'recoil';

// import { getBooks, getBookById } from 'src/web/api/books/api';
import { currentBookIDState } from '../atoms/booksAtoms';

// export const getAllBooks = selector({
//   key: 'GetAllBooks',
//   get: async () => {
//     const response = await getBooks();
//     return response;
//   },
// });
